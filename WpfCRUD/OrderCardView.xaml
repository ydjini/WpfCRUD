<UserControl x:Class="WpfCRUD.OrderCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfCRUD"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="767">
    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="220"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="30, 15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Margin="0, 0, 5, 0" BorderBrush="Black" BorderThickness="1"/>
            <Border Grid.Column="1" BorderBrush="Black" BorderThickness="1"/>
            <Grid Grid.Column="0" Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" FontWeight="Bold">
                <Run Text="Артикул: "/>
                <Run Text="{Binding Article}"/>
                </TextBlock>
                <TextBlock Grid.Row="1">
                    <Run Text="Статус: "/> 
                    <Run Text="{Binding OrderStatus}"/>
                </TextBlock>
                <TextBlock Grid.Row="2">
                    <Run Text="Адрес пункта выдачи: "/> 
                    <Run Text="{Binding PointAdress}"/>
                </TextBlock>
            </Grid>
            <TextBlock Grid.Column="1" x:Name="deliveryDate" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16">
            <Run Text="{Binding DeliveryDate}"/>
            </TextBlock>
        </Grid>
        <Grid Grid.Row="1" x:Name="cardBtns" Margin="30, 0, 30, 15" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding Id}" Margin="8" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <Button Grid.Column="1"
                Content="Редактировать"
                Margin="20, 5"
                Padding="10, 5"
                />
            <Button Grid.Column="2"
                Content="Удалить"
                Margin="0, 5"
                Padding="10, 5"
                Background="DarkRed"
                Foreground="White"
                />
        </Grid>
    </Grid>
</UserControl>
